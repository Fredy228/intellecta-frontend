import Schedule from "@/components/screens/schedule/Schedule";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Schedule | Intellecta",
};
export default function SchedulePage() {
  return <Schedule />;
}
